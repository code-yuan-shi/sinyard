#Mybatis Generator configuration

#根据数据库中的表生成对应的pojo类、dao、mapper
jdbc_url: jdbc:mysql://localhost:3306/test?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true
jdbc_driver: com.mysql.cj.jdbc.Driver
jdbc_user: root
jdbc_password: root

#dao类和实体类的位置
project: src/main/java

#mapper文件的位置
resources: src/main/resources
